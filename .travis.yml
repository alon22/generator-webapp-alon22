language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-webapp-alon22' ]; then cd .. && eval "mv $currentfolder generator-webapp-alon22" && cd generator-webapp-alon22; fi
